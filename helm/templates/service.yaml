apiVersion: v1
kind: Service
metadata:
  name: {{ include "litestar.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "litestar.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000
      protocol: TCP
